import React from 'react';

// Empty arrow component
const CustomArrow = () => null;

export const CustomPrevArrow = (props) => <CustomArrow {...props} />;
export const CustomNextArrow = (props) => <CustomArrow {...props} />;
